import { makeStyles } from '@material-ui/core/styles'

const styles = makeStyles((theme) => ({
  media: {
    height: '500px',
  },
  deleteIcon: {
    position: 'relative',
    top: '440px',
    right: '-230px',
    color: '#fc5603',
  },
  btn: {
    margin: '5px 2px',
    fontSize: '15px',
  },
}))

export default styles
